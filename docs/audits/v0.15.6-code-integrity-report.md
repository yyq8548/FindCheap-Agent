# FindCheap Agent v0.15.6 代码完整性报告

日期：2026-08-29

## 结论

活跃代码已收敛到 MCP、Awin Feed、Registry Builder、注册表所需 Postgres 层和插件包。Commerce API 与 Ingestion 数据平面没有近期部署入口、启用商家或活跃调用，因此已归档，不再参与 workspace、构建、Lint、类型检查、测试、Docker Compose 或 CI。

## 已完成清理

- 删除无引用函数、类型与测试转发壳，包括旧 Awin 校验入口、旧 Commerce 比较类型、测试专用 Shopify identity 转发层和未使用仓库辅助代码。
- 删除 MCP `commerce-client`、`ComparePort`、`compare_products` conditional tool、Commerce 环境检测及对应测试。
- 删除活跃依赖中的 `bullmq`、`ioredis`、`yaml` 和 MCP 未使用 contracts 依赖。
- 将 Commerce、Ingestion、旧 merchant adapter/SDK、pricing、ranking、product identity、Gold Set、旧 DB repository/fixture、Docker、runbook 和 platform CI 移到 `archive/commercial-platform/`。
- 保留历史 SQL migrations。它们属于已部署 Postgres 的迁移链，不能当普通死代码删除。

归档规模：131 个文件，952,781 bytes。归档内容由 ESLint、TypeScript、Vitest 和 pnpm workspace 明确排除。

## 统一版本与商品卡规则

- 单一运行时版本源：`config/version.ts`，当前为 `0.15.6`。
- 新增版本一致性测试，校验插件 manifest、Skill、OpenAI metadata 与运行时版本。
- `search_products.limit` 统一为最多 8。
- 三层固定上限：官网 2、可信匹配 3、高性价比高匹配 3。
- `LOWEST_PRICE` 只在每层内部排序，不再把三层合并。
- MCP 输出 schema 与商品卡 UI 同样限制最多 8 张。

## 文件拆分

- `search-products.ts`：1,604 行降至 1,420 行；候选排序、分层、限额与展示门移入 `product-candidate-ranking.ts`（196 行）。
- `server.ts`：3,371 行降至 3,171 行；商品卡 telemetry schema/type 移入 `product-card-telemetry.ts`（45 行），同时移除 Commerce tool 组装。

这次按低风险边界完成第一阶段拆分。工具注册仍集中在 `server.ts`；若继续拆，应按 Search、Visual、Selected Product/Quote、Deals、Watch 五个注册模块逐步迁移，每次保持 MCP 契约测试通过。

## 当前活跃功能

- 统一文字商品搜索：Awin、Shopify Global Catalog、eBay、已验证官网 fallback。
- 图片搜索：候选召回、Codex 当前会话视觉复核、一次受限放宽。
- 三层商品卡、稳定 `selectionId`、Shopify variant 检查与 ZIP 报价。
- 已验证优惠查询与用户明确授权后的 Watch。
- Railway Awin Feed、官方商家注册表、可信商家注册表和 Registry Builder。

## 验证结果

- MCP bundle：通过，652,521 bytes。
- Awin Feed bundle：通过，415,438 bytes。
- ESLint：通过。
- TypeScript：通过。
- 单元、契约、smoke：41 个文件、448 项通过。
- MCP stdio：2 项通过。
- `git diff --check`：通过。
- PostgreSQL 集成测试：未执行成功；本机 `127.0.0.1:5432` 无服务，Docker Desktop daemon 也未启动。失败原因为 `ECONNREFUSED`，不是代码断言失败。

## 剩余风险

- `server.ts` 仍有 3,171 行，后续应继续按工具域拆分，但不建议在同一批归档改动中继续扩大重构范围。
- SQL migrations 仍包含已归档 Commerce/Ingestion 表；这是迁移历史兼容，不代表对应运行时仍活跃。
- 完整 DB 集成门需要启动 Postgres 后补跑 `pnpm test:integration`。

## 发布状态

本次只完成本地代码修改、构建和验证；尚未提交、推送、发布或安装新缓存版本。
