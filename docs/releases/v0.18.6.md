# FindCheap Agent v0.18.6

This release repairs problems found while testing product search, selected-product inspection and Windows upgrades. Final local integration has passed; publication, deployment, installation and native acceptance are recorded separately below.

## Changes

- Treat a category request such as “Nespresso Original compatible coffee capsules” as category discovery. Compatibility wording stays in the requirements instead of becoming an exact product name. Budget, brand, model and selected-variant constraints remain enforced.
- Isolate every version of an Awin product key when its normalized records disagree across feeds. Fully identical records retain one copy; the service does not choose a conflicting price. Duplicate keys within one feed still fail validation. Health metadata separately records input, excluded and deduplicated rows.
- Preserve successfully validated Awin source-cache metadata when aggregate publication fails. Unchanged sources can be reused after retry or restart. Validation failures and the local request quota wait for the normal refresh cycle; temporary network failures retain bounded retries.
- Recover Shopify prices only from evidence bound to the selected product, variant, SKU, amount and currency. The inspector safely parses supported Web Pixels data without executing page scripts. Conflicting or ambiguous evidence remains unknown; parent prices and global currency labels cannot supply a variant price.
- Keep product-edition evidence attached to the current product. A medicube FAQ recommendation for a separate Mild product no longer makes the original SKU conflict with a “not Mild” requirement. Missing edition or net-weight evidence remains unknown.
- Compare same-product offers when one source repeats an already verified condition as a variant option. Unknown or conflicting conditions, different colors, packaging and identity conflicts still prevent comparison. Merchant trust is unchanged.
- Protect the existing Windows plugin cache before invoking the Codex installer. Restore missing old files without overwriting concurrent changes, and verify installed/enabled state plus the exact distribution file set and hashes. Installation failures remain failures.
- Correct GR-Research headphone/brand routing and the existing Orleans Coffee canonical origin. Add Hidden Secrets Wigs as a search source with standard priced color variants, bringing the access table to 1,003 stores. An explicitly false optional SKU becomes missing rather than invalidating the whole response; other invalid SKU values and product/parent/variant checks remain enforced. Redirect restrictions and merchant-trust boundaries remain unchanged.

## Evidence so far

The final candidate passed both builds, typecheck and lint, plus **207 files / 4,068 assertions** in the complete default suite. Windows PowerShell 5.1 installer behavior checks passed. The default suite includes bundle stdio checks and excludes external PostgreSQL integration; no database schema or migration changes are included. The first full run retained 4,062 passes and five failures: three stale version/merchant/origin assertions and two runtime-test synchronization failures. The corrected tests await the actual refresh-settled event; production retry delays and budgets were not increased. Three initial type errors were also corrected and rechecked.

Candidate package: `0.18.6+codex.20260909202818`. MCP SHA256: `8eb0247d61adaafe3bb67608f48d582ffbd296ae00001db4161696c984a03b8d`. Source-service SHA256: `9981ab1dc9c751c55e8092ea0eb4b3fd5b9d26888793847707f62fe087131ff0`. These identify local artifacts, not yet a production or installed-runtime claim.

Scoped builds and regression tests are recorded in the [query log](../engineering/changes/2026-09-09-query-compatibility-followup.md), [Awin log](../engineering/changes/2026-09-09-awin-refresh-followup.md), [selected-product and edition log](../engineering/changes/2026-09-09-host-acceptance-followup.md), [comparison log](../engineering/changes/2026-09-09-comparison-condition-followup.md), [Woo coverage log](../engineering/changes/2026-09-09-woo-coverage-followup.md) and [Windows implementation record](../engineering/changes/2026-09-09-remaining-completion.md). These are local or source-level checks, not a completed release-wide acceptance run.

The Awin regression run passed 1,200 assertions. A local replay of 50 real source caches retained 70,146 valid products across 35 merchants after isolating 530 conflicting groups and all 1,060 associated rows from 71,206 input rows. The group count is observed data, not a fixed exclusion list. The replay passed with the production-equivalent 4 GiB heap; a separate 512 MiB stress run exhausted the heap and remains recorded. Production data was not replaced by this replay.

Public selected-product reads recovered the exact Glossier Espresso and Allbirds size-5 prices without borrowing a parent offer. Those observations do not establish current checkout totals or successful native approval forms. Original failures, partial source coverage and missing evidence remain in the engineering records.

## Pending acceptance and delivery

- Commit, push, CI and release publication; production bundle/hash, health and a successful fresh Awin snapshot.
- Installed-package state, exact cache parity, stdio checks and actual native runtime verification.
- Native shopping, selected-product comparison, task recovery and approved quote-form acceptance. Source probes do not replace these tests.
- The fixed native performance matrix and any image development timing. The collector's historical verification does not establish the candidate's P50/P95 or a general 90/180-second guarantee; see the [performance plan](../engineering/changes/2026-09-09-native-performance-followup.md).

F09's original reference image and F15's 40 business tasks plus 40 human-labeled images remain deferred by the user. Automatic cleanup on task deletion and confirmed Watch notification delivery/stopping remain host-dependent. No model-supplied ACK, missing-task error or local state transition proves those events occurred. Unverified quote amounts, compatibility, stock and merchant trust remain unknown.

No checkout, payment or WooCommerce Cart capability is added. Earlier releases remain unchanged; see [v0.18.5](v0.18.5.md) for its separate delivery evidence.
