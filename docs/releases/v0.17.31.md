# FindCheap Agent v0.17.31

Runtime `0.17.31`; package `0.17.31+codex.20260907174748`.
[Research, alternatives, frozen plan and module evidence](../engineering/changes/2026-09-07-package-continuation-quote-feedback.md).

## Scope and safety

- Explicit package quantity/net content becomes separate hard requirements even when submitted in requiredSize. Physical/ambiguous sizes and overflowing constraint sets are not silently dropped.
- An explicit Mild confirmation continues the original goal and constraints. A longer edition title must match a candidate in the original snapshot while preserving original anchors. Request confirmation is not stable cross-merchant identity proof.
- Selected inspection exposes bounded safe reason/host, never raw provider exceptions; it still reads only the selected product and does not search a replacement.
- Both comparison views wait through the bounded consent/provider window, retain the table and selections on failure, and distinguish authorization, reference, eligibility and unknown outcomes. No automatic retry, duplicate click or stale-result overwrite. Unknown outcomes do not assert that no temporary Cart exists.
- Direct comparison hydration now counts as received content so the card-loading timer cannot erase it. Nine comparison rows unchanged.

No dependency, database, local shopping-memory/Watch policy, merchant trust or payment permissions change. No live Cart writes in verification.

## Release plan and rollback

Chris authorized changes and synchronization before manual retest. Publish a new patch/versioned cache rather than overwriting an in-use cache. Required: versioned builds, typecheck/lint/default tests/diff/stdio; GitHub commit/push and CI; explicit existing Railway production deployment and health/readiness/artifact hash; enabled installed-cache source equality and read-only same-goal replay. Report these separately.

Rollback: prior runtime commit `34982b5bea9c71916c894cceded44e15f65e2598`, package `0.17.30+codex.20260907164805`. No data migration. Identity/permission/selection regressions block release; revert/redeploy the prior version on verified regression, never forcibly overwrite a locked cache.

## Verification

Final local versioned gates on 2026-09-07: MCP/Awin builds, typecheck, default suite **133 files / 1,961 assertions**, bundled stdio **4/4**, and diff check passed. Final lint and delivery evidence are recorded below when completed. Resource URLs advanced to cards v37 / comparison v7 to invalidate old UI resources.

- MCP SHA-256: `725316970aeb0bb6c133fd256aa82c12ca8c19a665af688ed2d2618d35d73519`.
- Awin SHA-256: `9ae4923352caa5144d3340ef623e4d2d74e76ad8fe091d6651cfa660628166cc`.

Live development checks: pre-version original trajectory passed 8/8. Three subsequent independent versioned cases passed; the continuation still passed but exact inspection then encountered medicube HTTP 429. Cooldown observed, not bypassed. Inspection now classifies 429/5xx and does not amplify them with an HTML fallback (deterministic assertions passed). This failure is retained as evidence, not retroactively counted as a successful inspection.

Final lint passed. At 17:56 UTC one cooldown recheck of the final bundle still returned `RATE_LIMITED / SOURCE_RESPONSE / medicube.us`: 7/8 original-trajectory assertions passed, exact inspection remained blocked. No further medicube requests during this release. This is not a green live acceptance or a claim of full Test closure.

Release decision: **LIMITED GO for Chris's explicitly requested synchronization and manual retest**, not full shopping acceptance. Offline hard-invariant and release tests pass; the remaining observed failure is an upstream 429, with safe failure handling verified. No permissions, identity or source safety are relaxed to make the live check green. Installed stdio/hash verification remains required; post-install merchant inspection stays blocked by the documented upstream condition rather than being retried repeatedly.

GitHub/production/cache synchronization evidence pending.

Original screenshot quote error and historical inspection root cause remain unknown. Native consent dialog, actual merchant quote side effects, original-photo identity, independent accuracy and lifecycle acceptance remain unverified. The default suite excludes separate DB integration tests; DB contracts are unchanged.
