# FindCheap v0.18.3

Release preparation: **2026-09-09 UTC**. This patch expands WooCommerce discovery to 1,000 configured storefronts and repairs capsule matching, pagination, coverage reporting and bounded recovery. Production and installed-cache delivery are **PENDING** until the separate gates below have evidence.

Package: `0.18.3+codex.20260909031910`. Product-card resources advance to `v44`; comparison resources advance to `v14`. Existing running Codex processes need a reload to use the new package.

## What changes

- **More Woo stores, with the same request budget.** The source registry `2026-09-09-expanded-1000` contains the unchanged original 200 stores plus 800 new search-only admissions. Four qualified reserves stay outside runtime. Each addition has actual public USD product, exact selection, public-sale and US-market evidence. Public directory positions guide discovery priority; they do not prove every store is popular. See the [data summary](../engineering/changes/2026-09-08-woocommerce-expansion-1000-data.md) and [complete portable evidence](../engineering/changes/2026-09-08-woocommerce-expansion-1000-evidence.json).
- **Capsules stay distinct from equipment and incompatible systems.** Coffee clarification keeps the original goal and requirements. Capsule form and machine compatibility are assessed separately, including actual roast/brew single-serve-cup titles without the word coffee. Brushes, empty capsules, conflicting grounds and unrelated foods do not become capsule matches. Unknown machine compatibility prompts clarification and prevents a primary recommendation; K-Cup does not imply Nespresso Original compatibility.
- **A second pass can retrieve new evidence.** Shopify uses a query-bound next-page cursor when available. Repeated queries without a new page or useful query change report no increment. Coverage counts actual pages, stores and requests across passes; estimated source totals are not counts searched.
- **Partial Woo coverage no longer blocks every recovery path.** Explained store limits and unsupported-store responses allow otherwise eligible independent recovery within the existing budget. Security, permission and fatal failures remain enforced. Browser recovery still requires a server-issued action and host authorization.
- **Exact Woo prices and links stay bound to the selection.** Conflicting product/variant URL parameters and duplicate normalized attributes are rejected. Incomplete required choices and explicitly non-purchasable products retain discovery identity without a confirmed price or availability. Links containing cart, checkout, account or unsupported action parameters cannot become product-card URLs.
- **Older clients retain their response shape.** New clients opt into Woo coverage details with `x-findcheap-woo-coverage: 1`. Missing or unknown capability values keep the legacy strict store shape without changing the shared internal result.

No merchant-trust database change is included. The [prior 200-store trust review](../product/woocommerce-merchant-trust-200.md) remains separate from the larger access registry. Ratings and source admission do not grant merchant trust or primary recommendation status. No Woo Cart, checkout, order or payment support is added.

## Completed implementation evidence

The [implementation record](../engineering/changes/2026-09-08-capsule-search-and-woocommerce-1000.md) preserves the completed local work before release authorization. Those earlier records correctly state that production and the installed cache were not updated at that time.

- Both bundles, typecheck and lint passed; the final implementation checkpoint passed **182 files / 3,788 default assertions**, including four stdio checks. A separate **26/26** research-helper assertions passed under the Node runner. The opt-in database/legacy integration suite was not run or counted as passed.
- All **800/800** included additions replayed exact saved raw products through the current normalizer. The 1,000-store and historical 200-store subset passed **963/963** assertions. The final 981-store snapshot and 19-store delta received separate offline duplicate/catalog-label checks.
- Seven live local-controller product checks ultimately passed: five unchanged non-coffee observations at **03:05 UTC**, then two fresh corrected coffee observations at **03:11 UTC**. The initial two coffee failures remain in the evidence. Boston's Best returned its selected 32-pod child at USD 24.95 with machine compatibility unknown; Mean Mug returned its K-Cup product at USD 8.99 with an explicit K-Cup match and Original conflict. These are dated item-price observations, not delivered totals.
- At **03:12 UTC**, the local bundle retained one goal through coffee, capsules and Nespresso Original, with immutable older cards. Awin, Shopify and Woo participated. Awin was bounded-complete; Shopify and Woo were partial. Original had no confirmed compatible offer in that observation, not proof of global absence. That bundle used the previously deployed **200-store upstream**; the separate local controller verified the **1,000-store implementation**.
- The v0.18.3 version step preserved four expected old-version/resource failures before correction. Its **254/254** focused assertions and immediate source/MCP builds then passed. Final verification of the release package follows below.

## Verification and delivery status

User authorization covers commit, push, production deployment, cache replacement and the version release. Authorization does not itself prove delivery. The [release execution record](../engineering/changes/2026-09-09-v0.18.3-release.md) owns final command receipts and immutable artifact identities.

| Gate | Status and evidence to record |
| --- | --- |
| Final v0.18.3 package verification | **PASS**: source/MCP builds, 182 files / 3,788 default assertions, stdio 4/4, typecheck, lint and diff. MCP SHA-256 `f4eb9c401fc55a68a097fc162f77bd00dcc525d021e2db89ff0b9d4cc3be2565`; source SHA-256 `5578b3c9e0d39d6e465c4d744259fd252bd6988de96b0b74660f8858486165b8` |
| Commit, push and GitHub CI | **PENDING — RELEASE_GIT**: runtime commit, verified remote SHA and successful CI URLs |
| Production source deployment | **PENDING — RELEASE_PRODUCTION**: deployment ID/SUCCESS, deployed source hash, readiness, unchanged official/trust records and bounded response with 1,000 eligible stores |
| Installed and enabled cache | **PENDING — RELEASE_CACHE**: exact package, enabled state, source/cache file parity, raw MCP hash and installed stdio |
| Installed real-source dialogue | **PENDING — RELEASE_INSTALLED_LIVE**: current production-backed sequence, actual source coverage, compatibility and immutable-selection results |
| Tag and GitHub Release | **PENDING — RELEASE_PUBLICATION**: tag target, published release URL and actual installer assets |

Native Codex card interaction and approval UI remain separate, unexecuted acceptance states unless independently observed. SDK success must not replace that evidence. Historical failures and the pre-release undeployed interval remain preserved when delivery rows are completed.

## Try after installation

Restart Codex and open a **new task** after cache verification. In one task, ask “我要买咖啡”, then “胶囊咖啡”, then “我的机器是 Nespresso Original”. The goal should continue, old cards should remain unchanged, and incompatible systems must not become confirmed offers. Missing compatible results or incomplete coverage must remain explicit.

Use the [repeatable acceptance tests](../testing/capsule-search-and-woocommerce-expansion.md) for source coverage, selected variants and real-store checks. Current inventory and results can change. Woo searches select at most six stores per pass and at most two passes; neither a successful response nor an empty result surveys all 1,000 stores. Market evidence does not guarantee every US address, shipping cost, stock or delivery date.

## Rollback

Retain the successful v0.18.2 deployment and `0.18.2+codex.20260908215316` cache until v0.18.3 delivery is verified. Source deployment and plugin-cache rollback are independent. No trust-database migration is required. An occupied old cache can remain intact; restarting Codex loads the verified package rather than changing an already running MCP process.
