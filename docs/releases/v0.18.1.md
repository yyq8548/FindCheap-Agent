# FindCheap v0.18.1

The 200-store Woo access registry now has individually reviewed trust dispositions: **137 official brand stores, 45 established retailers and 18 stores retaining search access only**. Approved records are separate from access permission. Eligible Woo offers compete through the existing product, variant, stock, price and merchant gates; merchant approval alone never creates a primary recommendation.

Woo official URLs and brands use the existing Backend Woo catalog, with reviewed-host priority inside the same six-store/two-pass budget. Explicit product brands are preserved. Known mixed-brand merchants receive retailer status to avoid applying one brand to every product on their host. Old clients negotiate the supported official-registry subset; the full representation requires schema 2 and has an independent ETag. Both UI resources advance their cache identifiers because their embedded runtime version changes.

The [200-store review](../product/woocommerce-merchant-trust-200.md) links every merchant, decisive evidence and limitations. The [portable ledger](../engineering/changes/2026-09-08-woocommerce-trust-200-evidence.json) retains unsuccessful reads and all 200 technical probes: 197 PASS / 3 FAIL. The [explicit approval payload](../../config/registries/reviewed-woocommerce-trust-2026-09-08.json) contains 319 records: 137 official plus 182 trusted. Existing 111 official and 211 trusted records are preserved; the intended published totals are 248 and 393.

## Verification and delivery

Implementation and frozen step checks are recorded in the [change plan](../engineering/changes/2026-09-08-woocommerce-trust-200.md). The first registry assembly rejected redundant punctuation/accent aliases; normalization removed only aliases already equivalent to the canonical brand. The production database preflight passed read-only, with the previous release and all approved rows unchanged.

Final local builds, typecheck, lint and **162 files / 2,589 assertions** passed, including bundled stdio **4/4**. Package: `0.18.1+codex.20260908204926`. MCP SHA-256: `b3c809de4b5bf6973e8a384bd6de0a9b01b58876d0764c033333d07d1978f41d`. Source SHA-256: `d98795de79eb54f1e91e46a930b3c4ae8753fca8b38f6be42d4a9ba1395fb343`.

Native Codex card interaction and actual Watch/Automation scheduling remain separate from SDK checks.

## Delivered on 2026-09-08

- Runtime commit `f4fc7ecf8050a6a06c061449e727c01ee172157e` pushed to main; remote SHA matched. [Plugin CI](https://github.com/yyq8548/FindCheap-Agent/actions/runs/34278082601) and [Windows installer CI](https://github.com/yyq8548/FindCheap-Agent/actions/runs/34278082527) passed. No tag or GitHub Release was created.
- Compatibility deployment `cc9aba39-0fb4-40e4-baa5-145f42428115` reached SUCCESS; SSH source hash matched the artifact above. The reviewed 319 records were collected with their original technical probe results, approved and atomically published as `registry-2026-09-08-woo-trust` at 21:04 UTC. Complete payload comparison preserved the previous 111 official / 211 trusted records and old immutable release.
- Refresh deployment `2c10d1e4-7a83-4f3d-810a-8c9304c6d9c3` reached SUCCESS and retained the same source hash. Live registry checks passed **6/6**: health/readiness, legacy 111-store official subset, schema-2 exact 248-store official registry with 137 Woo stores, separate ETags/304, and exact 393-store trust registry. A subsequent read-only database audit independently returned `ALREADY_PUBLISHED_AND_VERIFIED`.
- Production source checks passed **8/8**: unchanged 200-store bounded search, two exact product URLs, selected paint variation, image proxy and unknown-merchant rejection, plus health/readiness. No feed refresh or unrelated service setting was changed.
- The existing Git marketplace upgraded. Plugin add reported Access Denied when backing up an occupied old cache; independently, plugin list confirmed **`0.18.1+codex.20260908204926`, installed=true, enabled=true**. All **13/13** new-cache files match source after line-ending normalization, raw MCP SHA matches, and installed stdio passed **4/4**. The occupied old cache was retained.
- Installed-cache live SDK passed **6/6** using the real production gateway and isolated test state. General Pencil, Restoration Games and Root Science each returned one exact `OFFICIAL_STORE` card and a READY primary selection. PINE returned one trusted-retailer primary, without an official badge. Alaska Honey returned one research-only card with `UNVERIFIED_MERCHANT` and no primary. Every direct lookup planned one of 200 eligible stores. No actual Watch or Automation was created.

Machine-readable [delivery evidence](../engineering/changes/2026-09-08-woocommerce-trust-200-delivery.json) records the separate runtime, database, service and installation states. Restart Codex and use a new task to load the new plugin process; follow the [user tests](../product/woocommerce-merchant-trust-200.md#可重复用户测试). SDK success does not establish native host card/selection acceptance.

## Rollback

Retain the previous immutable database release `registry-2026-09-07-medicube`, prior successful source deployment `5f484531-ae33-4cd4-8197-8d078a364460` and installed `0.18.0+codex.20260908165141` cache. Roll back the reviewed registry before returning to a source service that cannot parse the Woo platform. Preserve unrelated approved records and the 200-store access table. Existing Woo Watch state uses the unchanged v0.18.0 format.
