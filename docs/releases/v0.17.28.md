# FindCheap Agent v0.17.28

Runtime `0.17.28`; package `0.17.28+codex.20260906155224`. User-authorized code changes, commit/push, Railway production deployment and installed Codex cache update. [Research, alternatives, frozen plan and assertions](../engineering/changes/2026-09-06-wig-coupon-spec-contract.md).

## Changes

- Share selected-product Coupon assessment across raw card labels, ranking, comparison and deal research. Exact NO/N/A placeholder terms remain raw evidence; explicit sitewide offers can become conditional merchant candidates, never confirmed discounts. Wholesale, order thresholds, customer restrictions, unknown scope and conflicting terms still block promotion.
- Count unique merchant offers, not copies attached to several cards. Lead selected-deal text with the server-chosen candidate and eligibility limits; retain full source evidence in structured output. Skills reference the actual dealSummary field.
- Show bounded merchant-reported Awin variant suffixes only when the numeric variant URL matches the source product ID. Preserve spelling and unknown units; no invented dimensions, color normalization or quality certification.
- Prefer distinct styles in ordinary merchant-diverse category discovery. Fold subsequent source-bound sibling variants at their original positions, preserving each price and selection ID. Primary and selected variants remain expanded. Exact, explicit cross-store and visual search policies are unchanged.
- Keep nine comparison rows. Localize the new source-variant label; advance card/comparison resources to v36/v6.

## Safety and acceptance

No new dependency, provider, trust grant, local product catalog, persistent format, Cart side effect or permission bypass. Only product-confirmed applicable discounts may affect price. Grouping is presentation metadata, never identity or selection authority. External data fences, safe networking and one-use quote consent remain.

Known task replay and synthetic tests are development evidence, not held-out accuracy or checkout Coupon validation. Actual host authorization popup recovery, task-scoped restart memory/lifecycle, real Watch scheduler stop and independent visual acceptance remain unresolved.

## Verification

MCP and Awin builds, typecheck, lint and diff validation pass. Final default suite: **125 files / 1,828 tests**, zero failures or pending tests. Separate bundle stdio smoke: **4/4**. Default tests exclude database integration tests.

The recorded Awin-only task replay passes initial search, black/$100 refinement, UI selection sync, comparison and selected Coupon lookup. It retains three original products, 29 unique offers, source-reported specifications and stable selection, with zero Cart calls. IS18 remains conditional; WS30 is not promoted. Selected summary: 342 characters. Bounded live Ishow lookup returned 29 offers; actual checkout applicability is unverified.

- MCP SHA256: `1ce1f87c1d3b10a202b254aa4cedd84f1ba86dcda7189a9e3a3681dc092d443b`
- Awin SHA256: `8ee8b5643682402eb108a3f9af67ec75e130f40002e9f9ad9e8fb145203cf12d`

The first full suite exposed two skill-length failures and an obsolete summary field assertion. Wording was compressed within unchanged caps and the assertion corrected to dealSummary; no runtime gate was loosened. Publication destinations are verified separately after commit; this pre-commit document does not imply a completed deployment or installation. Restart Codex and open a new task to load the installed version.
