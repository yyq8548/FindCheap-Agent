# 第二批：集成测试与六图开发验收

本页保留发布前的时间点与原始产物证据。后续用户另行授权的版本打包和发布验证见 [v0.17.24](../../releases/v0.17.24.md)；下文“未提交／未部署”指当时交付状态，不覆盖后续发布记录。

**最终本地门禁：2026-09-06 07:10:29 -04:00，108 文件／1,594 项全通过。**
TypeScript、全局 lint、MCP／Awin 构建通过；07:10:46 独立 stdio 4/4，
diff 检查通过。最终本地 bundle SHA256：
`555a21847267afa686c754966d442cfc9818c96d9db85b441859fb5903afa833`。
下文保留修正前门禁和真实六图的原始 hash，不将它们冒充新 bundle 的实测。

## 交付边界

首批已发布：运行版本 **0.17.23**，包版本
`0.17.23+codex.20260906085854`，GitHub commit
`0a0b45cf8ac5250cbdf62b96c27d5a1a3b1bf9e2`。
Railway production deployment `efbece61-aa13-4a38-9f5a-3bfb153a8b37`
成功；健康、就绪、只读检索与部署产物 hash 已核验。
Codex 新版缓存已注册启用，13/13 文件一致、已安装 stdio 4/4 通过。
旧缓存被进程占用，未强制删除；重启 Codex 才能保证当前进程加载新版本。

**本页后续描述的是第二批本地未提交改动，不属于上述线上发布。**
没有再次提交、推送、部署、安装或提升版本。

## 五阶段与自动门禁

事实、两案比较及各修订的冻结顺序见
[实施记录](2026-09-06-design-alignment-batch-2.md)；
[图片证据](2026-09-06-batch2-visual-evidence.md)、
[预算证据](2026-09-06-batch2-budget-evidence.md)、
[Watch／报价证据](2026-09-06-batch2-watch-quote-evidence.md)
保留逐模块 RED → build → GREEN，不覆盖历史失败。

2026-09-06 06:56:37 -04:00 的集成检查：

| 门禁 | 结果 |
| --- | --- |
| 默认 Vitest 全套，非数据库 integration | 107 文件，1,589/1,589 通过 |
| TypeScript / ESLint | 通过 |
| MCP 构建 / Awin Feed 构建 | 通过 |
| stdio | 4/4；亦包含于上述默认全套，不重复计总数 |
| Skill 版本及契约 | 16/16；compare 6,397 bytes，Watch 3,699 bytes，未提高限额 |
| 差异空白检查 | 通过；Windows CRLF 提示不是测试失败 |

首跑 1,587 项中 4 项失败：无 options 的优惠端口多传 undefined、stdio
漏记新增可选语言字段、Skill 漏版本标识。分别恢复兼容调用、严格同步
schema 键、恢复版本标识；没有删除断言。压缩 Skill 的两次中间尝试触发
既有措辞契约，恢复所需措辞后 16/16 通过。

只读复核另复现两处“计时器未调度，过期请求仍发出”。各新增一条行为测试，
先红，再在 SearchRun 实际 dispatch 与网页 worker dispatch 前检查既有
单调截止时间；立即构建后 31/31、61/61，最后纳入 1,589 项全套。

## 六张原图：真实来源、带品牌、已知开发集

执行时间：2026-09-06 10:57:14–11:00:58 UTC。
固定本地 bundle SHA256：
`5a3e2131d7964eaa15bacbfa3acd9d04b47237d2d6e77e7e2ee626a0934587eb`。

使用实际 MCP stdio、实际配置的只读商品来源和返回图片。六个初次调用与此前
冻结输入逐字段一致，原图 hash 一致；只给品牌及图片观察，不注入 PDF 答案
URL／款名／商品代码。搜索完成后才读取本地答案表进行 URL 核对。
主执行者查看全部 **39 张候选图片**，提交实际视觉判断；最多两轮复核。
没有创建 Cart、Watch、Automation，没有模拟网页授权或用答案链接补结果。
共享来源客户端在同一独立测试进程运行，因此不是六次冷启动基准。

| PDF 图 | 实际终态 | 指定款式 | 指定配色链接 | 服务端流程时间 |
| --- | --- | --- | --- | --- |
| 1 Julia | 可能同款，所选变体有货 | 命中 | 命中 | 26.1s |
| 2 Faye | 可能同款，所选变体缺货；仅调研，无购买首选 | 命中 | 命中 | 39.0s |
| 3 Zeno | 已复核相似款，不宣称精确同款 | 命中 | 命中 | 26.9s |
| 4 Oren | 第二轮返回已复核相似款 | 命中 | 命中 | 51.5s |
| 5 Nataly | 0 卡；请求受授权网页补搜，未执行该补搜 | 未命中 | 未命中 | 28.1s |
| 6 Belden | 素色 CRM 相似款，明确与原图花纹不同 | 命中 | 未命中原 PGA 配色 | 40.2s |

计数：**款式 5/6，PDF 指定配色 URL 4/6，执行层确认 EXACT 0/6**。
链接命中是事后答案核对，不是图片身份已在运行时确认；款式命中不能把
不同配色算目标成功。Faye 只证明该返回变体缺货，不推断所有尺码均缺货。
全部流程仍有 128–154 秒剩余预算，未出现读超时；Nataly 的失败不能归因于
180 秒耗尽。有限官网候选和视觉门槛未带回目标，完整网页恢复尚未验收。

Nataly 的只读分层复核：首轮4张是 Adeline／Opaline／Camila／Maven，全部
成功加载，分别因领口或裙长冲突被拒绝。系统随后确实补跑4个未使用的官网
查询，不是完全没有二次检索；只有一个新规范化对象，因商品大类冲突在加载
候选图前被排除，另外3次返回0。该模式已延迟普通视觉文本过滤，故不能把
`OFFICIAL_VISUAL_EVIDENCE_INSUFFICIENT` 聚合标签解释成“图片已复核失败”。
现有 capture 没有暴露这个对象的标题／类别／URL，**不能证明被排除的是
Nataly，更不能断言安全门槛误杀了它**。下一步应先补官网发现／截断／解析
失败的分层计数和商品hash对应的大类排除原因码，再判断发现、前6页有界
读取、最多一页续页、PDP解析或分类哪层漏召回；不要先扩大门槛或造本地目录。

只读本地核验脚本 `artifacts/pdf-six-trial/verify-batch2-final.mjs` 检查
原始调用／原图／bundle一致性、复核数量、终态、可信且有货首选、缺货保留、
相似标记和原配色差异，结果 PASS。初次脚本误用不存在的 `VERIFIED` 枚举，
按实际 `INDEPENDENT` 契约修正，未改任何原始 capture。
原图、商家候选图、raw capture、核验脚本都留在 gitignored 本地 artifacts，
没有加入仓库。

这些是有先验曝光的开发样本，不是盲测、独立验收或总体准确率。仍不满足
[正式验收门槛](../../testing/visual-search-acceptance-v2.md)要求的冻结至少
40 张样本、带／不带品牌、基线／候选及独立 held-out 对照。

## 实测暴露的终态一致性问题

此固定 bundle 的图 4、6 已有 READY 相似首选，却附带“没有已核实符合商品”
的文本；图 5 零卡分支没有统一 `visualSearchOutcome`。原始失败保留，不把
它们解释成通过。

随后已按冻结 V8 完成小范围修正：通用 Chrome／无商品建议只用于文字检索；
零卡共用 helper 统一 remembered state、结构化输出、模型文字的视觉终态。
等待网页恢复／报告不完整都明确 incomplete，不声称商品不存在；初始零图
精简输出同步可选字段。具体失败原因、安全恢复权限、身份／颜色／排序门槛
不变。中英文 READY、初始零图、全冲突、网页图片失败、预算／两轮耗尽及
文字恢复回归完成；局部163/163，根最终全套1,594/1,594。

V8 没有重写六图 capture，也没有重新执行六张真实来源检索。因此六图数字
仅对应上文预 V8 固定 bundle；V8 的终态改动是自动回归通过，不是新的真实
图片准确率验收。Nataly 和 Belden 原配色问题没有因此被宣称解决。

## 整体尚未闭环

- 服务端 90/180 秒单调预算已实现；原生第二轮、网页补搜继承。不是完整宿主
  用户回合预算：重复初始工具调用仍可开启新 flow，宿主前后时间／P95 未验收。
- 图片兜底已实现，但 Nataly 召回和 Belden 原配色仍未达到六图目标。
- 跨重启比较记忆、归档／删除联动尚无已验证可信任务绑定，未伪造替代身份。
- Watch 本地原子保存、CAS、删除防复活、补货完成与 STOP_REQUIRED 已实现；
  真实宿主绑定、停止、通知送达及可靠来源准入仍未闭环。
- 报价具备 MCP 当前表单、作用域／短期单次许可、固定 API 与变体校验；真实
  桌面授权、商家 Cart 行为及副作用验收仍未通过，模拟断言不能代替。
- 未建设本地商品目录，未读取用户聊天数据库，未替用户创建实际监控。
