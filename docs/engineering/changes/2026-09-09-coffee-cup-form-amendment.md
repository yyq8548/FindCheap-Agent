# C2 follow-up: explicitly roasted/brew cup titles

Research: the final 1,000-store local controller acceptance on 2026-09-09 UTC found two genuine USD lookup observations incorrectly rejected as equipment: Boston's Best `Wicked Awesome Organic Ultimate Roast Single Serve Cups` (parent 15094, child 15095, $24.95, selected 32 pods) and Mean Mug `Meanest Brew K-Cups` (product 744, $8.99). Both have empty product categories. The existing coffee identity rule requires the literal word coffee or espresso before replacing cup-form terms, so their unconverted cups trigger the equipment exclusion. The original failed report remains in ignored `artifacts/woo-1000/verification/registry-live-final/report.json`; the two exact DTOs are retained in the versioned `woo-cup-form-sample.json` fixture.

Proposals: A would accept all K-Cup or single-serve-cup titles as coffee, risking unknown containers and other beverages. B adds a bounded title equivalence only when an explicit roast/brew term accompanies a recognized capsule-cup form. Root approved B before implementation. Descriptions, shop names, URLs and artificial brand allowlists cannot supply the missing identity. Selected pack count does not establish machine compatibility.

Frozen scope: add desired-behavior helper and registered MCP replay tests first, preserve their actual failures, then minimally change shared coffee identity. Explicit tea/hot chocolate, empty/refillable cups, holders, storage, machines, ordinary cups and selected-ground conflicts remain dominant. Naked capsule-cup terms or roast/brew alone cannot become matched coffee capsules. Public workflow must retain exact original prices/selected attributes, unknown-machine clarification, explicit K-Cup compatibility and immutable earlier snapshots. Test trust records are explicitly synthetic and do not admit the merchants to a real trust registry.

Validation: root serializes the immediate MCP bundle build; focused coffee and Shopify-classification tests plus scoped lint follow. No new merchant request, runtime network permission, source budget, registry/trust change, version or deployment belongs to this increment. This saved-real-data replay is not new live-source or native-host acceptance.

Implemented: three intended RED failures (two exact saved title assessments and one actual registered MCP zero-card result) preceded the change; twenty contrary/unknown controls already passed. The minimal shared rule now recognizes the conjunction of the title words roast/brew and an existing cup-form label. It does not change the later equipment/other-food/empty-capsule or selected-form checks.

Root's immediate MCP build passed. The new file passes 23/23 and all ten focused coffee files pass 238/238; scoped ESLint and diff checks pass. The public in-memory workflow preserves both actual DTO prices/selections, asks for the system with no primary, then verifies Mean Mug's explicit K-Cup title after the user specifies that system. Boston's 32-pod count stays compatibility-unknown and the earlier snapshot is unchanged. Logs are saved as `cup-form-red.log` and `cup-form-green.log` under ignored `artifacts/woo-1000/verification/`. Current live-source rechecks and the final full repository gates are independently owned by root.

The two existing Shopify client/Global Catalog test files also pass 43/43, for 281 focused assertions across 12 files; their separate log is `cup-form-shopify-green.log`.
