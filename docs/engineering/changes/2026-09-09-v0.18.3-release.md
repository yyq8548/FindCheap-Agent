# v0.18.3 release execution

## Authority and Research

On 2026-09-09 UTC, Chris explicitly requested commit, push, production deployment, installed-cache replacement, README updates and version release notes for the completed capsule search and 1,000-store expansion. This is release authorization for the existing repair scope; it does not approve shopping actions or merchant-trust changes.

Baseline main/origin/main: a4d51c7a3155756458f17aafcbf7e45cab82cfb4. Approved implementation is currently uncommitted and completed its final local gates: 182 files / 3,788 default assertions, 26 separate Node research assertions, seven final live products and bounded real-source dialogue. The previous cache is installed/enabled 0.18.2+codex.20260908215316. Current production deployment is 33e93f9e-2dc5-40e4-82d6-3b5df585694e, SUCCESS, in project 9bf7a976-a5ca-4524-bb9b-457d1a4750b5 / service FindCheap-Agent (2a177106-d0e0-4de9-a9fd-de6a4c258f2b) / production (c115fe92-33b0-45e2-b7f4-6af738c0f1b0).

Version consumers include config/version.ts, plugin manifest, distributed skill metadata, HTTP user-agent and embedded UI resource versions. Existing CI rebuilds and compares both bundles on Linux/Windows and runs plugin/installer checks. The Docker build includes only the source-service bundle at runtime; admission HTML stays excluded, and the tracked portable ledger is not imported by runtime code. GitHub access uses authenticated gh CLI. Railway 5.49.6 is available through npx; no global railway executable is on PATH.

## Proposals and frozen plan

| Option | Benefit | Tradeoff |
| --- | --- | --- |
| A: v0.18.3 patch, backward-compatible capability negotiation, new card resource/cache identifiers | Ships the requested corrections and data within existing endpoints, budgets and trust boundaries | Existing running Codex processes must reload the plugin |
| B: v0.19.0 minor with the same implementation | Signals a larger merchant-coverage milestone | Suggests a broader public feature/contract expansion than this compatible repair |

Choose A. Freeze package 0.18.3+codex.20260909031910, product-card resource v44 and comparison resource v14. Root owns version/resource changes, builds and release operations; README and release prose are delegated separately. Add no feature, quota, database migration or merchant-trust approval.

1. Update release expectations first and preserve the intended old-version/resource failures. Apply shared version, metadata and resource identifiers, then immediately build both consumers and run focused version/UI/plugin assertions.
2. Update README/release notes. Run typecheck, lint, rebuild, full default suite, bundled stdio and diff checks. Preserve prior failure logs. Never count default-suite success as the excluded database/integration suite.
3. Commit approved implementation plus release artifacts; push main and compare remote SHA. Wait for relevant GitHub CI success before explicit production deployment.
4. Deploy only FindCheap-Agent / production. Require SUCCESS, production hash parity, health/readiness, unchanged official/trust registries and one bounded Woo search proving eligibleStores=1000 and the new registry version. Preserve real source partial/failure diagnostics.
5. Upgrade the configured marketplace and install the versioned cache. Leave occupied old cache intact. Verify installed/enabled version, all tracked plugin files after newline normalization, raw bundle hash and installed stdio. Run the bounded installed shopping dialogue separately from native Codex UI acceptance.
6. Record final CI/deployment/cache evidence and release notes. Publish the requested version release with the actual version tag and installer artifacts if the repository release workflow supports it. Verify the release/tag target. Documentation-only follow-up is permitted without pretending it changed the deployed runtime hash.

Rollback keeps the former production deployment and old versioned plugin cache. Source and client rollback are independent. Native host popup/card acceptance remains unexecuted unless independently observed.

## Results

The initial release checkpoint had the gates below pending. They are now complete as recorded in Completed delivery. Detailed command receipts remain under ignored artifacts/release-v0.18.3; verified outcomes are also recorded in docs/releases/v0.18.3.md.

### Pre-release gates

The version/resource expectation run produced four intended failures and 77 passing controls before the version change. After applying the frozen identifiers, both immediate builds passed and the six-file version/UI/plugin subset passed 254 assertions.

The final v0.18.3 package then passed typecheck, lint, MCP/source builds, the full default suite (182 files / 3,788 assertions), separate bundled stdio (4/4) and diff checks. Logs remain under artifacts/release-v0.18.3. MCP SHA-256: f4eb9c401fc55a68a097fc162f77bd00dcc525d021e2db89ff0b9d4cc3be2565. Source-service SHA-256: 5578b3c9e0d39d6e465c4d744259fd252bd6988de96b0b74660f8858486165b8. These are package-local gates; remote CI, production and installed cache remain pending at this checkpoint.

### Completed delivery

Runtime commit `dce79c5fe9f230f3ef884d2349d5827786288968` was committed and pushed; the remote main SHA matched. Plugin CI `34307037208` completed successfully on Linux and Windows, and installer CI `34307037405` passed. The published `v0.18.3` tag resolves to this runtime commit.

Production deployment `cd40b90a-d42b-4730-9fbd-5f27f0bc6acf` reached SUCCESS. SSH verified `/app/dist/main.cjs` equals source SHA-256 `5578b3c9e0d39d6e465c4d744259fd252bd6988de96b0b74660f8858486165b8`. Health/readiness passed. Before/after GETs verified every official/trust field unchanged: 248 official records, 111 legacy-format official records and 393 trust records. The bounded Woo query reported registry `2026-09-09-expanded-1000`, 1,000 eligible stores, six planned stores, twelve HTTP reads and PARTIAL coverage. A store with retained products timed out; per-store failures remain part of the evidence despite the aggregate succeeded count.

Marketplace upgrade succeeded. `plugin add` reported `failed to back up plugin cache entry: Access is denied. (os error 5)` for the occupied old entry. The old cache was retained. Independent inventory confirmed `0.18.3+codex.20260909031910` installed/enabled; all thirteen tracked plugin files match source after newline normalization and the raw MCP hash matches. Installed stdio passed 4/4.

The installed, production-backed dialogue passed coffee, capsules, explicit Nespresso Original and immutable earlier rendering at 03:29 UTC. It observed the 1,000-store registry and retained three actual Shopify capsule/pod candidates. Unknown user system produced no primary; Original had no confirmed compatible offer in the observed bounded search. Woo timeouts and partial results were preserved. No browser approval or native host interaction was performed.

[GitHub Release v0.18.3](https://github.com/yyq8548/FindCheap-Agent/releases/tag/v0.18.3) is published. Tag installer workflow `34307449661` passed, attached CMD/PowerShell/ZIP/checksum files, and all three downloaded files match the published SHA-256 sums. The [machine-readable receipt](2026-09-09-v0.18.3-delivery.json) records exact runtime, delivery and coverage facts. A documentation-only follow-up records these outcomes on main without changing the version tag or deployed runtime bundle. Restart Codex and open a new task for user-visible testing; native host acceptance remains independent.
