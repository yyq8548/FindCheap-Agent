# v0.18.7 delivery record

## Research and authority

The baseline is published v0.18.6, commit `87f6f1c13d4a8217564b6e2049f691a4222a8aee`. The user has authorized completing controllable repairs, committing, pushing, publishing, installing and native testing. The original medicube image and human-labeled 40-task/40-image evaluation remain deferred. Native approvals, quote consent and the unanswered NF07 policy decision are not inferred from elapsed time.

A newly reproduced adapter defect sends a recognized Shopify URL to WooCommerce as a direct product target. The official reader already supplies compiled product text, but the foreign URL causes the Woo backend to reject the request before merchant selection. The public offline reproduction retains two failures and one passing text control, with no external network calls. The native R4 receipt reports SOURCE_REJECTED but does not capture the exact backend HTTP status. See the separate [routing repair record](2026-09-09-woo-known-shopify-url-followup.md).

The runtime imports `config/version.ts`; the plugin manifest and distributed skill labels share its semantic version. `packages/network-safety/src/safe-fetch.ts` also imports this version for the HTTP User-Agent and is consumed by the source service, so even this MCP routing patch requires rebuilding both bundles after the version change. The MCP package builds the committed plugin bundle, notices and metafiles. The source-service bundle is independently deployed. The Windows installer retrieves the main-branch distribution and verifies its complete inventory; release-tag CI publishes the installer assets. Existing tags and frozen test manifests are historical evidence.

## Propose

| Option | Delivery and compatibility | Cost and risk | Rollback |
| --- | --- | --- | --- |
| A: publish a new 0.18.7 patch | A distinct version identifies the routing repair and preserves v0.18.6 evidence. The installed distribution receives a new cache suffix. | Version-label updates, final tests, a normal installation and a scoped native regression. Existing pending native tests keep their original candidate binding. | Reinstall the previous tagged distribution; retain exact cache backups. |
| B: publish another 0.18.6 cache suffix | A fresh suffix can distribute the fix while the semantic release remains 0.18.6. | Makes the already published tag and later runtime behavior harder to distinguish; requires separate build identities everywhere. | Restore the prior suffix from the verified backup. |

Choose A under the standing release authorization. Do not rewrite v0.18.6 or restart pending native samples to avoid approvals.

## Frozen plan

1. **D1, after the isolated routing module passes:** update the shared version, plugin manifest and three distributed skill/agent labels to 0.18.7. Preserve all prompt text except the version label. Build MCP and its source-service consumer immediately, run version and distributed-skill assertions, then the default suite, typecheck and lint. No PostgreSQL schema or database behavior changes; the external PostgreSQL integration environment is not part of this repair.
2. **D2, after D1:** update both READMEs, the current improvement-plan entry, architecture implementation note and new release notes. Keep prior receipts and failure denominators. Verify local links, stated boundaries and `git diff --check`.
3. **D3, after D2 and independent review:** commit and push the candidate. Run the actual installer, preserve its exact backup, verify installed/enabled state and every distribution byte, synchronize already resolved compatibility entries only after backup verification, and run the installed stdio suite with its actual root and hash recorded. Never claim an existing host connection reloaded from a disk comparison.
4. **D4, after D3:** run one new native Glossier routing regression on the new code. Record actual runtime version, Woo source result, official selected variant and inspection. Preserve partial results and approvals; this is a functional regression, not the frozen performance matrix or quote consent. Pending R3/R4 tasks remain bound to their original evidence.
5. **D5, after required local checks and delivery verification:** deploy the newly built source-service bundle once if its hash changes, observe deployment health and the actual feed-publication result, then publish a new immutable v0.18.7 tag and verify CI and downloadable Windows assets. A successful deployment does not by itself prove a successful catalog refresh. Preserve existing snapshots and normal quota/error handling; no quota bypass or retry loop. Record remaining native/manual gates honestly. If the rebuilt source hash is unexpectedly unchanged, verify the build's version binding before considering reuse of the prior deployment.

The repair must not change Woo merchant exploration, trust, backend URL acceptance, permissions, source budgets or shopping identity rules. A newly observed independent defect requires its own diagnosis and bounded plan. A required-check failure stops dependent publication; the first failure remains recorded.

## Execution

Prepared package `0.18.7+codex.20260909224501`. Both consumer builds and the shared-version smoke check passed. MCP SHA256 is `4b165546210dd53e83739e14ccb261e3358d6af8acd09f832d579cf6c5ab8eaa`; source SHA256 is `190fbbef4a104f9288f0b0728d4edad7c410cdd3ba5621c740e23ced9d7b3bf9`. These are built candidate identities, not installation or production evidence.

The first final suite retained **4,118 PASS / 5 FAIL / 0 pending**, across 211 files, in `artifacts/remaining-completion/v0187-full-tests.json`. All five failures arise from literal 0.18.6 labels in the User-Agent, plugin-version and card-metrics contract tests. D1 now explicitly includes updating those nine current-version literals in four test files to 0.18.7. The tests retain their original behavior, equality checks, permission boundaries and budgets; no assertion is removed or weakened. Typecheck and lint passed before this test-only correction. A new complete report is required after it.

Plan v2 amendment before D1: independent release preflight found the source service's indirect version import through `safe-fetch.ts`. Both consumer builds and the conditional production delivery above are now required. The initial assumption that the source bundle would probably remain unchanged is withdrawn before changing the version or building either final artifact.

The corrected final suite passes **211 files / 4,123 assertions / 0 failures / 0 pending** (`v0187-full-tests-r2.json`). Typecheck and lint passed again after the test-label edits. Independent review confirms that all nine edits are current-version expectations, not historical rejection fixtures. All ten protected R4/diagnostic evidence hashes remain identical. Seven delivery documents and 146 local links pass validation. The routing module separately passes 7 new, 192 related and 6 independent offline assertions. Current source SHA for `search-products.ts` is `e1eaa2d1929fa3fc19e6c0b4bb054df17a510b930ee108a82bc4fcfb0ea18f30`.

D4 uses the same Glossier selected-product URL as the already completed R4 functional sample, so it directly checks the repaired behavior on the new candidate. The pending Sony S01 case is not restarted or substituted. Any approval prompt in the regression remains a real host gate; no parameter, URL, account or transport is changed to avoid it. Native runtime evidence remains separate from the disk hashes above.

## R1 delivery and native result

Commit `a651e819b1c522bb11e449b87ae1abea13118091` is pushed. [Plugin CI](https://github.com/yyq8548/FindCheap-Agent/actions/runs/34414180034) and [Windows installer CI](https://github.com/yyq8548/FindCheap-Agent/actions/runs/34414179988) both succeeded. Installer 1.2.1 completed at 22:50:59 UTC with the distribution installed and enabled. Its backup is `C:/Users/chris/AppData/Local/FindCheapAgent/cache-backups/eddff0f9643146e08055f03e6666ea06/recovery.json`.

The first compatibility preflight rejected an unexpected existing current-version entry before copying anything. Inspection found only the exact current 0.18.7 package in the installer backup, plus an empty host-resolved 0.18.4 directory; the intermediate old-version directories were absent. The independent v0.18.7 helper was corrected to accept that exact manifest version, preserving all path, backup, byte and inventory checks. It then verified two roots / 28 files. The original rejection and resolution are in `v0187-sync-preflight-resolution.json`; no directories were deleted by the helper. Installed stdio ran with the actual 0.18.7 root, passed 5/5, and retained its start/end, exit code and SHA binding.

Exactly one source deployment succeeded: `7bc1f901-77c3-44df-82df-3e07c65b9678`; the actual container SHA is the R1 source hash above. The snapshot at 22:52:34.658 UTC satisfies `71,208 = 70,148 + 1,060 + 0` (input = published + excluded + deduplicated), with 530 conflicting groups, 50 feeds, zero stale sources and zero consecutive failures. The actual refresh-settled event at 22:52:54.605 UTC records ready and no quick retry. Health/ready were each read once after settlement. Distinct merchant count was not recomputed. `v0187-production-final-verification.json` preserves the detailed result and earlier preflight evidence.

Native task `01a08861-a701-7863-9224-262d3112be49` completed in 73.581 seconds. Its actual runtime 0.18.7 search and inspection both succeed on the precise Glossier Espresso variant, USD 16 and in stock. Woo executes 8 reads / 7 completed merchants / 1 CONTENT_TYPE failure, with zero Woo products. The NF10 routing defect is closed; coverage remains PARTIAL. The actual header/body behind the failure is unknown. Native artifacts are `glossier-v0187-business-receipt.json`, `glossier-v0187-native-audit.json` and the manifest. The collector was corrected to preserve the raw AgentMessage content structure instead of reading a nonexistent text property; the final body is validated against the actual rollout.

## Plan v3: approved R2 category priority

After observing the native Espresso/coffee-store routing, the user explicitly approved category priority. This is a new business rule, not an inferred approval of the original NF07 zero-relevance proposal. R1 was delivered and tested but no v0.18.7 release tag exists, so retain semantic 0.18.7 and use a new R2 cache suffix after these changes.

Research confirms that ordinary text, continued requests and resolved Shopify URLs already forward the top-level productType through the Woo DTO and controller. A legal visual request can supply only `visualInput.productType`; the current Woo request omits this nested type. The minimum fix forwards top-level type first, then the existing validated visual type, only in the Woo request. It does not write inferred type back into stored hard requirements or change public DTOs. Backend classification must distinguish compatible, clearly conflicting and unknown metadata; neighboring categories are not automatically incompatible.

The two implementation routes are a narrow controlled category vocabulary within the existing planner or a new hierarchical registry schema with a data migration. Choose the former: no new schema, no registry/trust migration, conservative UNKNOWN for unsupported semantics. Detailed backend and visual steps are frozen in their separate engineering records before implementation.

R2 sequence: (1) backend planner/controller and public assertions, with source build; independently (2) visual type forwarding and public assertions, with MCP build; (3) independent reviews, new cache suffix, final dual builds/default suite/typecheck/lint; (4) README/design/status updates and link/diff checks; (5) commit, push, actual installation, byte verification and installed stdio; (6) one separately recorded source deployment and same-input native regression; (7) final v0.18.7 tag and release assets. Keep all R1 and R3/R4 matrices, snapshots, failures and pending approvals. A zero-store plan caused solely by explicit category conflicts completes the bounded query without claiming complete registry coverage or issuing an empty-progress continuation. Unknown-category exploration remains bounded at two per pass; unconfigured sources and real upstream errors retain their distinct states.

## R2 local verification

Package `0.18.7+codex.20260909231515` is built. MCP SHA256 is `3f175106fc4096780bcbf47257927cd64843879a65cd5c21b674c27a74be1ad0`; source SHA256 is `14ce0c4ef5bfb944042659cf8c6e7e6fcf6d34894bf34db52bd9b7983f85fa1e`. The final suite passes **213 files / 4,159 assertions / zero failures or pending tests**. Both builds, typecheck, lint and the version smoke check pass. Category routing passes 31 focused tests, 931 related assertions and ten independent probes. Visual-type forwarding preserves its initial 3 PASS / 2 FAIL reproduction before five corrected public contract tests and 108 related assertions pass. No external PostgreSQL integration was run; no schema or database behavior changed.

The local verifier checks nine documents, 149 local links, ten protected R4 evidence files and twelve frozen R1 evidence files. Original failures and candidate identities are preserved. Actual R2 installation, production verification and final native execution are the next delivery gates; successful local tests do not close them.

The old Sony, medicube and R4 S01 tasks resumed after genuine approval and completed. Sony's three-turn clarification, continuation and same-product comparison pass; medicube preserves Original / 70 pads / 155 g / excluded Mild but cannot complete the requested comparison. S01 retains valid Sony cards with unknown stock disclosed. The ten-check audit is `native-resumed-completion-business-audit.json`; runtime 0.18.6 and unknown approval-wait durations remain bound to those old samples. R4's latest append-only binding is one completed / 29 NOT_RUN, not 29 observed empty searches. These are no longer pending approval gates and are not R2 acceptance.
