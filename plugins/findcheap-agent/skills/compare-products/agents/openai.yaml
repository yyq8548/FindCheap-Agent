interface:
  display_name: "FindCheap Agent v0.6.14"
  short_description: "Trusted merchants, products, deals, and watches"
  default_prompt: "Use $compare-products to search Shopify products and render verified cards without launching MCP directly."
