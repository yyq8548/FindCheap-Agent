interface:
  display_name: "FindCheap-Agent v0.1"
  short_description: "Search five Shopify stores once; prefer merchant diversity"
  default_prompt: "Use $compare-products to search five Shopify stores once, return three merchant-diverse lowest prices, and use Chrome only after complete zero-result coverage."
