interface:
  display_name: "FindCheap-Agent v0.1"
  short_description: "Search public merchant sites with authorized Chrome"
  default_prompt: "Use $compare-products to search the web once through my authorized Chrome."
