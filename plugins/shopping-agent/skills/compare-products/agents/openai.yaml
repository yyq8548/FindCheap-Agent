interface:
  display_name: "FindCheap-Agent v0.2.1"
  short_description: "Exact-first matching across ten Shopify stores"
  default_prompt: "Use $compare-products to search ten Shopify stores once, return exact matches before labeled similar products, ask for missing model or variant details, and use Chrome only after complete zero-result coverage."
