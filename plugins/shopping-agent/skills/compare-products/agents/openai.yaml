interface:
  display_name: "FindCheap-Agent v0.2.3"
  short_description: "Exact-first matching across audited Shopify stores"
  default_prompt: "Use $compare-products to call search_shopify_products exactly once. Always request Top 3: use LOWEST_PRICE for explicit cheapest requests and MERCHANT_DIVERSE otherwise; format the first complete response, preserve returned order, report registry coverage, and use Chrome only after complete zero-result coverage."
