interface:
  display_name: "FindCheap-Agent v0.3.4"
  short_description: "Verified product cards and safe purchase links"
  default_prompt: "Call search_shopify_products once with no preamble; then call render_product_cards once with renderId. Never launch MCP."
