interface:
  display_name: "FindCheap-Agent v0.2.3"
  short_description: "Exact-first matching across audited Shopify stores"
  default_prompt: "Use $compare-products to search the audited Shopify registry once. Always request Top 3: use LOWEST_PRICE for explicit cheapest requests and MERCHANT_DIVERSE otherwise; preserve returned order, report registry coverage, and use Chrome only after complete zero-result coverage."
