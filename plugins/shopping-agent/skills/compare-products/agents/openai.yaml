interface:
  display_name: "FindCheap-Agent v0.1"
  short_description: "Use merchant APIs first, with Chrome fallback"
  default_prompt: "Use $compare-products to choose an applicable merchant API first, or authorized Chrome for a bounded web-wide search."
