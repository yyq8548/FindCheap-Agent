interface:
  display_name: "FindCheap-Agent v0.1"
  short_description: "Search Shopify first; Chrome only if empty"
  default_prompt: "Use $compare-products to search Shopify first and use authorized Chrome only when Shopify returns zero products."
