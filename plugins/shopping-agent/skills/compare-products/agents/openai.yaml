interface:
  display_name: "FindCheap-Agent v0.1"
  short_description: "Search public merchant sites with authorized Chrome"
  default_prompt: "Use $compare-products to find the best three verified options through my authorized Chrome."
