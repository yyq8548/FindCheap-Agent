# FindCheap 开发入口

## 先读什么

设计、修改或评审检索、图片匹配、商家信任、排序、比较、Coupon、Watch、会话记忆或执行安全时，先读 [Agent 总设计](docs/architecture/agent-design.md) 的适用章节和实现状态表。业务目标以其中批准的规则为准；当前能力以源码和验证证据为准，不能把目标当作已实现。

所有改动先读并执行 [工业级智能体研发协议](docs/engineering/agent-development-protocol.md)：Research → 至少两案 Propose → 冻结 Plan → 逐模块 Implement 与立即局部构建 → 自动断言 Test。各阶段产物、退出条件及纯文档验证边界以协议为准，不跳步。

## 工作规则

1. 遵循总设计；出现冲突先说明，涉及业务规则或权限变更由用户确认后再更新设计和实施。
2. 简洁优先，只改本次需求直接涉及的部分；保留现有适配器和无关用户改动。
3. 业务规则变化时，同步总设计对应章节、状态与回归测试；历史计划只作背景。
4. 验证范围随风险调整，不豁免协议规定的局部构建和相关断言；所有必选验证全绿才算该任务闭环，真实购物验收仍按总设计独立判断。
5. 分开报告设计批准、代码完成、真实验收、提交与部署。仅在用户明确要求时提交、推送、部署或替换安装缓存。

回复与卡片样式见 [DESIGN.md](DESIGN.md)；安装和使用见 [README.md](README.md)。
