# Agent 改进计划 9_14

计划版本：v1，2026-09-14。
状态：**v0.18.8 可控代码、自动验收与发布交付已完成。** 2026-09-14 后续已获用户明确实施与发布授权；P0–P6、可自动执行的 P7 项目和 P8 已完成。原生宿主及已暂缓人工集按真实边界单列。版本为 `0.18.8+codex.20260915013729`。

基线：v0.18.7，`02db6fbd622d96c9f3e4319146ab4771d90fd12a`。
工作区已有未提交的[结构审查报告](docs/engineering/2026-09-14-agent-architecture-review.md)，保留原文件与审查证据。
业务基准：[AGENTS.md](AGENTS.md)、[Agent 总设计](docs/architecture/agent-design.md)、[DESIGN.md](DESIGN.md)、[研发协议](docs/engineering/agent-development-protocol.md)。

## 1. 目标与完成口径

目标：把已发现的专项保护收敛成可复用的要求与证据规则，使同一需求在不同来源、搜索、检查、比较和恢复中得到一致判断；随后整理编排职责，完成可执行的真实验收。

三个交付包：

| 交付包 | 范围 | 可以得出的完成结论 |
| --- | --- | --- |
| A：共享判断规则 | 通用否定、完整复合要求、所选规格证据；多来源与派生流程复用 | 已发现的三类逻辑漏洞修复，相关自动合同通过 |
| B：输出与结构 | 最终结论一致、快照与编排职责收拢、设计文档同步 | 可控的输出与结构改进完成；模型最终文案仍以实际回执验收 |
| C：业务与宿主验收 | 实际商品、跨来源、性能、原生选择/恢复/报价；区分外部覆盖缺口 | 按场景报告 PASS/FAIL/BLOCKED/NOT_RUN，不自动等于整体设计达标 |

必须同时守住两条：

1. 明确冲突、缺少必需证据、借用其他商品/变体证据，不能进入首选。
2. 有足够正确证据的商品仍能进入合格结果；不能靠全部 UNKNOWN、全部隐藏或关闭来源完成修复。

原 medicube 图片、40个业务任务及40张真实图片的人工标注继续按用户决定暂缓。它们是整体设计验收条件，不计通过，也不冒充交付包 A/B 的已完成项。

## 2. Research：已知事实与待核实项

### 已知事实

- 当前共享执行器、Backend ports、任务作用域、持久化、身份锚点、最终资格检查已经存在，应保留。
- 实際链路：`search_products → evaluateSearchProductRequirements → evaluateProductRequirements → evaluateFeature → ranking → rememberSnapshot`。
- [公共 MCP 探针](artifacts/architecture-review-2026-09-14/public-mcp-probe.json)使用真实业务代码、模拟来源，三个负例都错误返回 READY/SATISFIED：
  - 明确 not waterproof → 满足 waterproof；
  - 16GB RAM / 256GB SSD → 满足“16GB RAM and 512GB SSD”；
  - 选中8片装，描述另售70片装 → 满足70片。
- 同次正例正确返回 READY。相关既有测试3文件73/73通过，说明现有测试没有覆盖上述缺陷。
- 最终文案已有 SF Bay 研究卡与“已排除”说法不一致的原生失败记录。
- `server.ts` 同时拥有工具、目标、快照、授权和输出职责；`search-products.ts` 混合编排与语义。
- 总设计第3节当前责任图滞后；第11节明确为历史基线，不应直接覆写。

### 实施前必须补齐的核查

| 未知项 | 核查方式 | 处理 |
| --- | --- | --- |
| 每个来源保留哪些原字段与变体标识 | 逐一核对 Shopify、Awin、Woo、官网和网页恢复的转换及消费者 | 无法绑定的字段保持未知，不猜字段所属商品 |
| 必选、排除、偏好各有哪些现有特殊合同 | 反查公共要求评估器的调用者、测试与总设计 | 先保留已批准语义；业务变化单列决定 |
| 共享代码是否被来源服务直接消费 | import 与打包入口核查 | 只构建受影响消费者，禁止凭文件位置猜测 |
| 旧持久快照包含哪些评估与原始证据 | 核对序列化、恢复、派生检查 | 历史事实不覆写；新判断不继承旧权限 |
| 原生工具、浏览器与审批在当前宿主的能力 | 开始实际验收时实时检查 | 当前记录不证明未来宿主能力；不绕过审批 |

本计划不声称已经枚举所有动态消费者。P0完成后冻结准确文件清单和测试用例清单，再开始实现。

## 3. Propose：两种可行方案

| 维度 | 方案 A：在现有评估器内集中修复 | 方案 B：小范围提取“要求表达—证据—评估”，由原接口调用 |
| --- | --- | --- |
| 位置 | 主要修改 product-requirements 与 matcher | 保留原公共接口，内部拆出可复用的表达和证据模块 |
| 优点 | 改动少、交付较快、兼容成本低 | 否定/组合/取证规则有独立职责，可复用于不同来源与流程 |
| 风险 | 匹配分支继续增多，先后顺序容易影响语义 | 初期涉及更多消费者，必须防止双轨规则长期共存 |
| 测试 | 公共接口验证最终效果 | 同一公共接口验证，另测内部规则不变量 |
| 数据/外部权限 | 无需新服务或数据迁移 | 同样无新增外部服务；优先不改持久格式 |
| 回退 | 撤回具体逻辑增量 | 按内部模块与消费者增量回退，不回退无关功能 |
| 适用范围 | 很小且独立的单条规则修复 | 当前已出现多品类、多个入口共享缺陷 |

**推荐方案 B。** 只围绕已证实的需求提取少量模块，不建设通用自然语言平台或新后端。先完成错误判断修复，结构整理另一步进行；不得把旧规则与新规则各维护一套作为最终状态。

## 4. 冻结的行为规则

### 4.1 完整要求，不丢条件

内部保留原用户要求和解析后的结构：原子条件、AND、OR，以及明确否定。复用现有数组/长度等输入限制；内部也必须有深度与规模上限，具体数值在P0按现有合同冻结，不提高外部预算。

- AND：所有子条件满足才满足；明确不满足或证据冲突阻止合格推荐。
- OR：至少一个完整分支满足；所有分支均明确不满足才判不满足，其余未知。
- 同一属性有互相冲突且同等适用的证据，保持 CONFLICT。
- 解析不完整、连接词歧义、无法解释的剩余文本保持 UNKNOWN；不得只取第一个数字或第一半句话。
- 单独字段和同一句话中的等价完整要求，结果应一致。
- 不实现无限制自然语言理解；受支持表达明确列出，未支持表达如实降级。
- 模型可帮助解释用户输入，不能用模型自报的“已满足/已验证”替代来源证据。

### 4.2 证据必须属于当前商品和所选规格

规范化事实须保留：属性、值/单位、正负陈述、原来源与字段、商品/变体绑定、适用对象、检查时间。复用已有身份引用，不添加可被模型伪造的“可信”开关。

取证顺序是同一属性下的适用性判断：

1. 已核实的所选变体字段优先。
2. 明确属于当前商品且适用于所选变体的字段才可补证。
3. 未选变体、推荐商品、FAQ对照、条件性宣传不能补成当前规格事实。
4. 对同一所选商品仍有无法解释的矛盾，保留 CONFLICT；不能机械地选择一个字段消掉冲突。
5. 大小包装、数量、重量、容量、颜色、机器系统使用同一机制；品类规则只提供受控解释。
6. 可选规格未知不阻断原本合格的商品；必需规格未知不得进入首选。

数量不能把“每包/包数/总数”混为一谈；数量、净重、容量和单位价格分开。只有对应数量及单位关系有证据时才换算，不猜测 ml 与 g 的密度，不把8包乘法自动解释为8片。

### 4.3 必选、排除、偏好共用事实，各守原合同

- 必选：有正确证据才能满足；必需证据未知不进入首选。
- 排除：证明当前商品具备被排除属性时拒绝；明确“不含X”不因出现X关键词而误拒绝。
- 对排除项缺证据，沿用已批准的领域合同，不擅自新增“一切排除条件都必须证明不存在”的全局门槛；也不宣称未知属性已经排除。
- 偏好：有证据才加分，不冒充硬条件；不能影响已确定的身份或冲突结论。
- 特定品类若需要更严格的排除规则，必须列为业务决策，不能藏在本次重构内。

### 4.4 统一最终结论，保留真实未知

搜索、恢复、检查及比较应消费同一组最终评估事实。首选只能来自当前合格卡片；推荐、数量、分组和缺失证据必须相互一致。

- READY、MATCHES_AVAILABLE、RESEARCH_ONLY、NO_MATCH各保持既有含义。
- 研究卡仍在结果中时，文案不能写“已排除”。
- 未返回容量、重量等事实，不能称“已核验”。
- 商品价不等于运税总价，有限搜索不等于全网最低价。
- 旧卡片不可变；新判断产生新快照，保留原顺序与引用关系。
- 工具层可以保证结构化结论一致；宿主最终自由文本仍须独立验收，不能承诺插件能拦截所有模型输出。

## 5. Plan：步骤、依赖、改动与通过标准

下表是实施顺序。计划文本已固定为v1，但不代表代码实施已启动或发布获批。P0补齐实现清单后，若发现需要改变业务规则、公开接口或持久格式，应记录原因并修订计划。

| 步骤 | 前置 | 改动范围与最小增量 | 验证入口 | 退出条件 |
| --- | --- | --- | --- | --- |
| P0 基线与冻结 | 无 | 固定源码/构建/原始失败；列全受影响消费者、场景与预期；把打印探针改为独立业务断言 | 公共 MCP search_products；原四个样本及既有73项 | 三个负例在旧行为上确实失败、正例通过；枚举用例在实现前冻结 |
| P1 完整条件与否定 | P0 | 原公共要求接口内使用有界要求表达；依次处理否定、AND/OR、数量类型和单位 | matcher/requirements公共接口＋MCP搜索 | 不再将not waterproof判为满足；复合要求每项完整判断；正反例与未知分支均通过 |
| P2 所选规格证据 | P1 | 规范化字段作用域、商品/变体归属及冲突；从颜色/咖啡已有保护推广到数量/重量/容量 | 公共MCP＋现有inspection接口 | 8片不能借70片描述转正；正确70片可匹配；证据缺失、矛盾与换规格状态正确 |
| P3 多来源与全流程复用 | P2 | 适配器输出消费同一事实合同；核对Awin/Shopify/Woo/官网/网页恢复，以及检查/比较/恢复 | 来源合同＋实际工具串联 | 同等事实得到同等结论；缺字段保留未知；初搜拒绝的条件不能经检查/恢复重新转正 |
| P4 输出结论一致 | P3 | 收拢现有最终投影；复用现有推荐、状态与证据字段，必要时只作兼容扩展；同步说明 | 最终卡片、模型回执、comparison输出合同 | 计数、分组、首选和文案事实一致；不靠追加提示词代替结构化校验 |
| P5 职责整理 | P4 | 分别提取目标/快照应用逻辑与输出投影；将商品知识和来源编排分离 | 既有公共合同、快照/取消/并发/隔离/授权回归 | 行为不变、没有双轨规则、没有新循环依赖；改一个证据规则不需修改多个工具handler |
| P6 设计同步与集成 | P5 | 更新agent-design第3节与当前状态入口；保留历史失败与边界；整理规则→代码→测试映射 | 全量相关回归、构建、文档链接 | 可控代码范围全部通过，已知未完成项明确列出 |
| P7 原生与业务验收 | P6 | 绑定最终候选包，按冻结样本实际运行，人工动作与宿主依赖分开 | 实际Codex工具/UI、源回执和时钟 | 分场景报告结果；未完成项目不计通过 |
| P8 发布交付 | P7及发布授权 | 确定版本、README/release、提交推送、按变更部署和安装验证 | GitHub/来源服务/安装包各自证据 | 批准的发布范围全部独立核验；不能以本地构建推定线上更新 |

P1内部按“否定 → 组合 → 数量”逐项红绿；P2按“字段归属 → 优先级/冲突 → 数量单位”逐项验证；P5一次只抽出一种职责。每个增量完成立即局部构建，不能多模块积压到最后。

### 构建与测试命令

- MCP代码或分发说明变更：`pnpm build:mcp`，紧接本步骤相关用例。
- 共享代码确实被来源服务消费或修改来源服务时：再运行`pnpm build:awin-feed`及其消费者合同。
- 核心逻辑集成：`pnpm typecheck`、`pnpm lint`、`pnpm test`、`pnpm test:mcp-stdio`。
- 最终版本固定后核验安装版stdio与源码/产物对应；源码测试不能替代最终包。
- `pnpm test:integration`按相关集成配置和环境运行；外部依赖缺失保留BLOCKED/NOT_RUN，不能当默认测试已覆盖。无关数据库不连接、不迁移。
- 纯计划/设计文档：构建N/A；链接、状态约束和`git diff --check`。

本计划写作阶段不执行上述产品测试。本次计划文档检查与未来实现检查分别记录。

## 6. 测试矩阵与防止“只修样例”

P0逐行冻结输入、独立预期、来源证据、规则编号和验证入口。开发用例可增加，但冻结的验收分母不事后删除；新发现单独追加编号。

| 规则族 | 必测正例 | 必测负例/未知 |
| --- | --- | --- |
| 否定 | waterproof；明确支持属性 | not waterproof；without；not only的非否定语义；其他商品的肯定句；不能可靠解析的否定 |
| AND/OR | 所有AND项满足；OR完整一支满足 | 第二项冲突；第二项未知；嵌套/标点歧义；禁止只判断第一个数量 |
| 选中规格 | 唯一绑定70片；明确所选512GB | 8片/70片串项；父级列多个规格；所选属性缺失；同所选对象字段矛盾 |
| 单位与数量 | 70 pads/70片的受控等价；明确质量/容量换算 | 每包数与总数混用；容量当重量；裸数字；包装大小信息缺失 |
| 需求角色 | 必选、排除、偏好分别按规则使用同一事实 | “不含X”误判含X；偏好代替必需条件；未知被宣称已核实 |
| 上下文 | 改预算保留身份/规格；显式换规格重新核验 | 重启/恢复/检查绕过硬条件；错误父引用；跨任务引用 |
| 输出 | READY对应真实合格卡片；正例有推荐 | 研究卡说已排除；缺规格说已核验；错误计数；商品价冒充到手价 |
| 既有安全 | 原权限、信任、身份和网络边界正常工作 | 引用注入、过期授权、改名绕预算、私网/重定向、供应商失败 |

泛化检查采用可判定的变换：更换品牌/商家/无关描述不应改变同一事实判断；交换AND项顺序结果不变；增加另一商品的肯定句不能改变当前否定；同义中文/英文与等价拆分输入结果一致。预期来自独立业务规则，不复制实现算法。

共同来源使用Shopify/Awin/Woo的真实数据形状模拟，官网与网页恢复另测真实入口；不强迫来源生成本来没有的字段。至少覆盖护肤、咖啡、电子、服装/装备，并保留正例，防止一律拒绝导致表面全绿。

这些属于开发与回归集，不替代暂缓的40业务任务/40图片独立人工验收。

## 7. 原生验收与已有欠账

| 项目 | 本计划处理方式 | 通过标准/限制 |
| --- | --- | --- |
| 三个新共享规则漏洞 | 核心必修，工具回归后用真实可获得商品复核 | 正确事实与返回状态一致；模拟通过不冒充现实商家实测 |
| Sony精确商品与跨店比较 | 作为正向防回归场景 | 型号/颜色/货况/身份/价格口径正确，未知库存明示 |
| Glossier规格表述、SF Bay研究卡 | 输出一致性必测 | 最终文案仅引用回执事实，研究/排除与实际卡片一致 |
| medicube原版70片/155g | 保留单独商品覆盖调查 | 核查实际SKU及至少两家同规格证据；找不到时报告覆盖/证据不足，不改规格过关 |
| 原生选择、桌面重启、报价表单 | 由用户实际完成所需动作，工具核验回执 | 模型传ID、模拟metadata、自动拒绝不代替真实动作 |
| 性能 | 重新绑定最终候选并冻结30文字与6张现有可用开发图；原8位置的2项不可运行状态保留 | 报告主动时间、用户等待、总墙钟、首卡和来源阶段；保留0结果/失败/未运行。90/180秒目标与P95结论分开，样本量限制明示 |
| 来源CONTENT_TYPE/超时 | 收集原始安全诊断，独立修原因 | 无原响应不猜根因；预算耗尽不等于商品不存在 |
| NF07零相关完全不探索 | 不在本次改变 | 延续已批准品类优先与有限未知探索；需另行业务决定 |
| 删除事件与通知送达ACK | 保持宿主依赖 | 不伪造自动清理或通知已送达证明 |
| 原medicube参考图、40+40人工集 | 继续暂缓 | 保留未验收状态，不计通过 |

P7是未来执行步骤，不在计划编写时创建新任务、发起搜索或要求用户操作。涉及原生工具审批时，才说明实际宿主要求与具体动作；不预先增加审批流程。

## 8. 风险、兼容与回滚

- **过度保守导致召回下降：** 每个负例同时有可满足正例，单独统计合格推荐覆盖与零结果。不能为了召回放宽硬条件。
- **规则与证据转换丢字段：** 保存原始用户表达及来源标识；转换合同核验数量、变体、来源与观察时间；缺失不猜。
- **公开接口变化：** 优先复用现有输入输出。必须扩展时先兼容消费者，不改变旧枚举含义或缩减允许行为。
- **旧历史与新规则：** 旧快照作为历史保留；新检查用新规则建立子快照。旧结论不能恢复授权或把旧价格刷新成当前事实。尽量不迁移持久格式；确需迁移则先修订计划。
- **重构风险：** P5与P1/P2行为修复分开，优先撤回结构调整而保留已验证修复。整包回滚v0.18.7会恢复已知误推荐漏洞，必须明确报告这一后果，不能称安全修复仍在。
- **模型文案风险：** 能控制工具的事实合同，不能保证宿主每个自由文本句子。原生失败保留，验证不足不得写“强制保证”。
- **外部数据与宿主风险：** 功能正确不保证商品现货、全网覆盖、通知送达或宿主能力；分别报告依赖。

## 9. 每阶段交付与发布门槛

每阶段记录：规则ID、修改位置、受影响调用者、首次失败、修复后结果、构建版本、未完成项。

交付包A/B完成至少要求：

- 三个原始负例全部拒绝错误首选，正例仍能获得正确推荐。
- 冻结的规则矩阵、适配器合同、派生与恢复安全回归全部通过。
- 新逻辑只保留一个权威评估入口；领域词表不单独决定信任、价格或推荐。
- 同步总设计与当前结构图；明确代码完成、原生验证、外部依赖和暂缓验收。
- 对最终候选的必选构建、测试与安装检查完成，不能用旧产物证明新代码。

正式发布时另列源码提交、远端推送、GitHub Release、来源部署（仅受影响时）、插件安装缓存/启用状态、哈希与stdio结果。发布授权按用户明确指令执行；当前计划不启动发布。

整体设计是否达标，继续按总设计第13节判断。即使交付包A/B完成，宿主依赖或被暂缓的独立验收仍未通过时，不称“Agent全部完成”。

## 10. 本次计划文档记录

- Research：回读当前审查、原探针、研发协议与现有工作区；源码基线未变。
- Propose：对比现有模块内集中修复与内部少量模块提取，推荐后者。
- 文档方案：A在历史9_9计划持续追加；B建立本次独立计划并链接旧审查。采用B，保留旧分母与批准状态，避免把新建议写成旧任务已批准范围。
- Plan：本文件v1固定目标、依赖、合同、验证及权限范围；生产实现尚未开始。
- Implement：仅编写本计划文档，不修改生产代码、提示词或工具配置。
- Test：仅链接/结构/状态约束与diff检查；无运行产物，无产品构建。

## 11. 执行与自动验收结果（2026-09-14）

本节记录获批实施后的结果；第10节仍是计划编写当时的历史记录。详细规则、代码和证据映射见[实施记录](docs/engineering/changes/2026-09-14-requirement-evidence-remediation.md)。

| 阶段 | 状态 | 验收结果 |
| --- | --- | --- |
| P0 基线与冻结 | **PASS** | 保留原失败探针；独立业务断言能正确区分正负样本 |
| P1 完整条件与否定 | **PASS** | 否定、AND/OR、多数量、单位类型及未知分支通过 |
| P2 所选规格证据 | **PASS** | 选中8片不能借70片父描述转正；冲突与未知保留 |
| P3 多来源与全流程复用 | **PASS** | Shopify/Awin/Woo及搜索、恢复、比较合同 4/4；真实 Woo 2/2 |
| P4 输出结论一致 | **PASS** | 返回计数、研究卡含义、首选和模型回执使用同一结构化事实 |
| P5 职责整理 | **PASS** | 快照投影和任务状态应用已抽出；公开合同与全量回归通过 |
| P6 设计同步与集成 | **PASS** | agent-design、DESIGN及实施记录同步；build/typecheck/lint通过；全量 4184/4184 |
| P7 可自动执行部分 | **PASS** | 公共 MCP 4/4、Woo实时集成3/3、性能1/1、stdio 5/5、采集器自检15/15与32/32 |
| P7 最终安装版原生矩阵 | **NOT_RUN** | 尚无获批并安装的最终候选；30文字/6图片不能用旧v0.18.7代测 |
| P7 暂缓人工集 | **NOT_RUN** | medicube原图、40业务任务和40真实图片继续按用户决定暂缓 |
| 通用数据库集成 | **BLOCKED** | 本机PostgreSQL `127.0.0.1:5432`未运行；Woo相关实时集成已单独通过 |
| 宿主依赖 | **BLOCKED** | 删除事件与通知送达ACK缺少宿主接口 |
| P8 发布交付 | **PASS** | 运行提交`de7ecb2`已推送；双CI通过；Railway部署`9c8e23bd-cc14-42f2-8c95-87d4eaa9e73b`成功且运行哈希匹配；v0.18.8已安装启用，14/14文件一致，安装stdio 5/5；Release按最终交付记录发布 |

结论：可控代码范围没有已知失败，v0.18.8已完成自动验收与发布交付。最终安装版原生矩阵、暂缓人工集和宿主依赖仍不计通过，因此不宣称整体 Agent 已全部验收完成。Railway代码部署成功且服务可用；启动Feed刷新因上游来源超过安全大小上限而保留旧快照并标为degraded，不冒充刷新成功。
