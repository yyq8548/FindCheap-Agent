# Archived commercial platform

This directory preserves the former Commerce API and ingestion data plane as historical source only.

- Archived from active development on 2026-08-29.
- Last active-tree baseline: `5ec0ee0`.
- Excluded from the pnpm workspace, TypeScript build, ESLint, Vitest, Docker Compose, and CI.
- Not deployed by FindCheap Agent.
- Restore from Git history and re-audit dependencies, migrations, secrets, and operations before reuse.

The active Registry Builder, Postgres registry tables, Awin Feed Service, MCP server, and plugin remain outside this archive.
